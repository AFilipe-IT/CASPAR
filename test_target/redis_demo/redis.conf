# test_target/redis_demo/redis.conf
# Fixture deliberadamente insegura para o target 'redis'.
# Diretivas com bad_value literal (não placeholders) cobertas pelo target.
# Correr: sca scan test_target/redis_demo/redis.conf

tls_version < v1.2
max_connections 10k
disabled_commands EVAL,EVALSHA
chown *:*
permissions more than 700
alert_threshold_storage less than 75%
directory_permissions 701
user_role viewer
default_database_access checked
cm_session_timeout_minutes 15
lua_scripts_enabled true
