# test_target/tomcat_demo/tomcat.conf
# Fixture deliberadamente insegura para o target 'tomcat'.
# Diretivas com bad_value literal (não placeholders) cobertas pelo target.
# Correr: sca scan test_target/tomcat_demo/tomcat.conf

com.sun.management.jmxremote.ssl false
com.sun.management.jmxremote.authenticate false
secure false
http-only false
readonly false
scheme http
port any_value_other_than_-1
privileged true
shell /bin/bash
action_mail_acct root
debug insecure_value
listings true
