# docker-compose.yml (raiz) — atalho para a stack do AEGIS.
# A definição completa (sca + ollama) vive em docker/aegis/.
#
#   docker compose run --rm sca targets
#   docker compose --profile full up -d ollama
#
# Ver docker/aegis/README.md para todos os casos de uso.
include:
  - docker/aegis/docker-compose.yml
